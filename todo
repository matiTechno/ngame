opengl wrapper classes: most important *texture && *shader ---- done
renderer --- done for sprites
font loading
sprite ---- done
text
pp_unit

-- now everything will be much better and simpler
then
entity component system with mini reflaction and cache friendly mem access
